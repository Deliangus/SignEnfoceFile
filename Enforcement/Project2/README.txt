Yuxiang Luo

List of files submited:
    Core.h : enum cless that defines most of tokens
    main.c : main function to parse an input file
    Scanner.c : calss used to scan and take tokens from input file
    Scanner.h : Interface between Scanner and main function
    tester.sh : testing script
    parser.h: the script that parse and execute the programming language.


Brief comments:
    The Scanner takes an input file and analyze all tokens at the beginning.

Known bugs:
    Doesn't return error message for code missing semicolon when it is needed.
